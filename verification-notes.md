## 2026-08-13 — mobile logo and layout check

На ширине 390px новый логотип отображается узнаваемо и без прежних лишних полей. Высота hero-блока уменьшена, заголовок стал легче и помещается в две строки, карточка настройки начинается сразу после hero. Кнопки тем расположены в две колонки, поле имени и переключатель таймера остаются доступными для касания. Основная страница продолжает корректно прокручиваться вертикально; нижняя часть настройки доступна ниже первого экрана.
## 2026-08-13 — start screen revision check

На мобильной ширине 390px боковой блок полностью удалён, инструкция размещена над тестом в компактной светлой карточке, заголовок «Тест на знание прайса Доктор Фармер» читается в две строки. Поле «Фамилия Имя (обязательно)», режимы, настройки и кнопка располагаются последовательно и доступны для касания. На десктопе стартовый экран сужен до центральной рабочей колонки 840px; верхний hero-блок уменьшен и получил светлый фон с сохранённой фотографией поля.
## 2026-08-13 — symbol-only logo check

Знак без букв подключён в шапке и отображается компактно. На мобильном экране он занимает только небольшой участок в верхней строке, не пересекает границы hero-блока и не вытесняет контент. На десктопе знак также остаётся внутри линии верхней конструкции; подпись knowledge lab / 2026 сохранена отдельно и не является частью логотипа.
## 2026-08-13 — compact question card revision

Карточка вопроса получила отдельный класс answered-state. После ответа вертикальные отступы, интервалы между вариантами, пояснение и кнопка перехода уменьшены; кнопка получила sticky-позиционирование внутри карточки, чтобы оставаться доступной при прокрутке длинного содержимого. Общая стартовая страница на мобильном и десктопном экране визуально не пострадала.
## 2026-08-13 — header alignment check

Шапка стартовой страницы теперь использует max-width 840px, совпадающий с центральной конструкцией hero и setup-card. На десктопе левый край знака совпадает с левым краем hero, а правая граница подписи «Внутренний тренинг команды» совпадает с правым краем hero. На мобильном экране служебная подпись скрывается, знак остаётся внутри горизонтальных отступов страницы и не выступает за блоки.
## 2026-08-13 — subtitle update

Подзаголовок hero-блока заменён на «Проверка знаний и продуктовой линейки ДФ.» На мобильном он помещается в одну строку с комфортным размером, на десктопе сохраняет спокойную иерархию и не меняет высоту конструкции заметным образом.

## 2026-08-14 — фиксированный таймер и ситуационный пул

На стартовом экране шестой режим «Сопоставление» заменён на «Ситуации»; отображается 6 из 6 тем. Фиксированное время рассчитывается без буфера: 20 вопросов — 10 минут, 30 — 15 минут, 50 — 25 минут, 100 — 50 минут; режим «Все доступные» — 30 секунд на вопрос. В итоговую выборку добавлено равномерное распределение по активным пулам с перемешиванием порядка после отбора. По окончании общего времени варианты ответа и кнопки перехода становятся недоступными; добавлен короткий двухтональный сигнал через Web Audio API с безопасным fallback, если браузер блокирует звук. `pnpm check` и `pnpm build` завершились успешно. Стартовый экран визуально проверен на 1280×720 и 390×844; новый пул корректно помещается в мобильную сетку.

## 2026-08-14 — очистка дублирующего заголовка и ФИО

Дублирующий блок с названием теста и подписью удалён полностью; пустого места не осталось. Поле «Фамилия Имя» поднято сразу под инструкцию, а счётчик тем перенесён в строку «Темы теста». На десктопе и мобильном экране компоновка остаётся аккуратной, все шесть тем и фиксированное время доступны. Для имени на экране результата отключено обрезание через ellipsis: добавлен перенос длинного ФИО на несколько строк с ограниченной шириной.

`pnpm check` и `pnpm build` завершились успешно.
